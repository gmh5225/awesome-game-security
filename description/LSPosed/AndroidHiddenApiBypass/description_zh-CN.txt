Android Hidden Api Bypass 是一个用于绕过 Android 非 SDK 接口限制的 Java 库，使隐藏的框架 API 可在运行时访问。
它提供 HiddenApiBypass 和 LSPass 两种变体，并提供用于调用受限方法和构造函数、读取隐藏字段以及管理豁免前缀的 API。
该实现不依赖原生代码，并按现代 Android 依赖工作流进行打包。
它常用于高级 Android 插桩、兼容性工具以及安全研究。
