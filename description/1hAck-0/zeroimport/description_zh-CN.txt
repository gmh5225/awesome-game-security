ZeroImport 是一个面向 Windows 内核驱动的 C++ 库，在运行时解析导入，而不依赖静态导入。
它通过对导出名称进行哈希，并遍历 ntoskrnl 的导出来定位函数与变量，例如 MmIsAddressValid 和 PsInitialSystemProcess。
该设计可减少导入表痕迹，并避免在编译后的驱动中嵌入明文 API 字符串，同时可选缓存以提升性能。
它主要用于重视隐蔽性与抗静态分析能力的底层驱动开发与安全研究。
