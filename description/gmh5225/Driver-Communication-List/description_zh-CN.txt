本仓库是一份精简的参考列表，汇总了用户态到内核态的调用路径，可用于研究 Windows 图形与 UI 入口如何进入更底层的内核组件。
其归档的 README 聚焦于具体示例，例如 `win32u.dll` 调用经 `win32k.sys` 与 `win32base.sys` 解析，最终到达 `dxgkrnl.sys` 中的目标，因此更像一本映射笔记，而非传统代码项目。
该仓库的价值在于这些调用链示例本身，它们为追踪通信路径提供了快速线索，便于随后在调试器或反汇编器中进一步分析。
它主要面向希望从 syscall 路径与驱动通信面入手进行研究的 Windows 内核与逆向工程研究者。
