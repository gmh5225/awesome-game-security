本项目是一个会话空间驱动映射器，它将驱动映像手动加载到会话内存中，而不走常规的内核模块加载路径。
归档 README 将其描述为将会话空间中的驱动映射；该方案由内核组件与客户端配合，客户端传入原始映像数据以完成导入修复、节区拷贝、重定位与入口点执行。
其主要挂钩会替换内部的 `ntoskrnl` 回调路径，为目标映像分配会话空间内存，解析导入与重定位，并在卸载时清除部分加载器元数据，以降低已映射驱动的可见性。
它主要适用于研究将会话空间手动映射、替代驱动加载流程，以及如何从普通加载器结构中隐藏已映射模块的 Windows 内核研究者。
