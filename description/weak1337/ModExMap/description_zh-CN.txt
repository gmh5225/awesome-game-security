一种用户态 DLL 注入器，通过解析 PE 头、使用 VirtualAllocEx 分配内存、经 WriteProcessMemory 写入各节区、解析导入表、应用重定位，并由 CreateRemoteThread 注入的 shellcode 桩执行入口点，将 DLL 手动映射到目标进程。
该映射器同时支持 x86 与 x64 目标，支持 TLS 回调，并包含 PE 结构解析（pestruct.h），用于在手动映射过程中遍历节表、导入目录与重定位块。
主要用于游戏安全研究人员学习用户态手动映射注入技术，以及与反作弊规避相关的 PE 加载内部机制。
