Módulo del kernel de Linux para Android que oculta el acceso root enganchando syscalls, operaciones VFS y el sistema de archivos proc mediante ftrace y tracepoints, y falsificando la información de montaje, el contexto SELinux y los atributos de archivo para eludir la detección de root.
