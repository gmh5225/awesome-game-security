Android ARM64 可加载内核模块，通过在加载时绕过 CFI 与 kprobe 黑名单，实现隐蔽的进程内存访问。它同时支持软件页表遍历，以及借助 ARM64 AT 指令的硬件地址转换；通过 phys_to_virt 进行直接物理内存读写（单次操作最高可达 50 MB）；支持绕过 VMA 结构的 PTE 级映射注入；以及进程间 DMA 缓冲区共享。通信可采用 IOCTL 或内核态套接字协议，并可选隐藏模块与信号。
