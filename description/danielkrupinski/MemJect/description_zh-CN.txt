一个精简的 Windows DLL 注入器：将已编译的 DLL 以原始字节数组嵌入源码，再通过 VirtualAllocEx、WriteProcessMemory 和 CreateRemoteThread，完全在用户态将其手动映射到目标进程（csgo.exe）。
映射器会解析 PE 头以在目标中分配合适大小的内存，复制各节区，通过 LoadLibraryA/GetProcAddress 解析导入，应用重定位，并在调用 DllMain 后可选擦除 PE 头与入口点，以减少取证痕迹。
它主要面向游戏安全研究者，用于学习用户态内存中的 DLL 注入技术与 PE 手动映射。
