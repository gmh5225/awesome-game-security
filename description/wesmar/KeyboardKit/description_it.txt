Progetto di ricerca su un keylogger Windows che usa un driver filtro del kernel per intercettare l’input da tastiera tramite IRP hooking, con un componente in user-mode per la trasmissione dei log via UDP e persistenza tramite hijacking della DLL ExplorerFrame.
