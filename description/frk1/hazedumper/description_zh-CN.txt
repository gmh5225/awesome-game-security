自动更新的 CS:GO 偏移量与 netvar 仓库，以多种输出格式（JSON、TOML、YAML、C++ 头文件、C#、VB）发布 engine.dll 与 client.dll 的内存特征码及解析后的偏移量。配套的 config.json 定义字节模式签名及相对/绝对寻址方式，覆盖 dwClientState、dwEntityList、dwLocalPlayer 等结构以及 m_iHealth、m_vecOrigin 等 netvar 偏移，便于外部内存读取类作弊程序在游戏更新后保持可用。
