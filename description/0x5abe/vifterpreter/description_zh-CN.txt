Vifterpreter 是一个 Rust 库，用于解析 PlayStation 2 的 DMA 数据包及其所携带的 VIF1 向量接口 VIF 命令。它建模 DMA 标签、标签标识符以及包括 unpack、微程序加载和状态命令在内的多种 VIF 操作码，并使用 binrw 进行二进制 I/O，使用 bilge 处理位域布局。Serde 支持使解析后的结构可序列化，便于检查或接入工具链。该 crate 面向需要解码 PS2 资产流（例如嵌入在二进制游戏文件中的网格或图形数据）的逆向工程师和游戏安全研究人员。
