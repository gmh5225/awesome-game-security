该项目是一款 IDA Pro 插件，通过共享的 git 仓库，在多名逆向工程师协作同一二进制时同步 IDB 数据。
每次保存时，它将名称、类型、内联函数及其他 IDB 数据导出为 YAML，再通过 git 提交、推送与拉取变更，并借助 git mergetool 自动解决合并冲突。
插件以输入文件的 MD5 标识 IDB，实现跨用户同步，并面向频繁、低干扰的同步场景设计。
它主要适用于需要在同一二进制上协作、又不想完整共享数据库的逆向团队，提供轻量级的 IDB 同步能力。
