本專案是一個 Windows API 呼叫混淆框架，透過核心驅動轉送使用者模式作業。
它以 C/C++ 實作，並將許多檔案、行程、記憶體、登錄與網路動作透過 DeviceIoControl 的 IOCTL 處理常式對外提供，而非直接呼叫 API。
儲存庫也包含多個範例用戶端，示範以 APC 為基礎的注入、驅動程式載入與通訊端通訊等實務工作流程。
本專案旨在支援底層安全性研究，以及針對 API 監控規避與行為分析強化的受控實驗。
