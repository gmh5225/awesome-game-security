本项目是一个 Windows API 调用混淆框架，通过内核驱动转发用户态操作。
它使用 C/C++ 实现，并将大量文件、进程、内存、注册表和网络操作通过 DeviceIoControl 的 IOCTL 处理程序暴露出来，而非直接调用 API。
仓库还包含多个示例客户端，演示基于 APC 的注入、驱动加载和套接字通信等实际工作流。
该项目面向底层安全研究，以及针对 API 监控规避与行为分析加固的受控实验。
