Microsoft の signtool.exe 向けの DLL サイドローディング悪用手法。XmlLite.dll をハイジャックし、WriteProcessMemory を用いてメモリ上で CertVerifyTimeValidity（crypt32.dll）および GetSystemTimeAsFileTime（KernelBase.dll）をパッチし、戻り値をゼロ化することで Authenticode 署名時の証明書有効期限チェックを回避する。
