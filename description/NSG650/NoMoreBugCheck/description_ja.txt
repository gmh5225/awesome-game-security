このプロジェクトは、KeBugCheckEx をパッチすることで標準の Windows BSOD 処理を抑制するカーネルドライバーです。
クラッシュ入口の動作を上書きし、致命的なエラーが通常のバグチェック経路を直ちに引き起こさないようにします。
実装は Windows カーネルモード向けに C および C++ で記述され、直接のコードパッチと復元ロジックを備えています。
主に Windows 内部構造とカーネルフッキングの実験であり、安全機構を迂回することのリスクを示します。
