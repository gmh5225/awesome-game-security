Un loader Linux per PS5 che sfrutta vulnerabilità del kernel e dell'hypervisor per avviare Linux su PlayStation 5, con configurazione IOMMU, inizializzazione della GPU, shellcode per sconfiggere l'hypervisor e gestione della TMR (Trusted Memory Region).
