PE 파일의 Authenticode 다이제스트를 계산하는 C 라이브러리로, 해싱 시 PE 체크섬 필드와 보안 디렉터리를 건너뛰는 해시 제외 알고리즘을 구현합니다. BCrypt API(SHA-1/SHA-256/SHA-384/SHA-512)를 사용하고, 임베디드 WIN_CERTIFICATE 구조를 파싱하며, 커널 모드(ksecdd.lib)와 사용자 모드(bcrypt.lib) 환경 모두를 위해 설계되었습니다.
